package org.example.Entities;

/**
 * Vendor entity removed from persistence (table `vendors` was dropped).
 *
 * This placeholder class remains to avoid compile errors if any legacy code
 * references Vendor types. The class is intentionally not annotated with
 * JPA annotations so it will not be mapped to any table.
 *
 * If you are certain there are no remaining references, this file can be
 * removed completely.
 */
// Vendor entity removed — file cleared on 2025-11-02 as requested by the user.
